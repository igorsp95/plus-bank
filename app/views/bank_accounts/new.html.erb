<div class="my-card">
  <h1 class="text-center">Abrir Nova Conta</h1>

  <%= simple_form_for(@bank_account, :html => { :class => 'form-horizontal items-form mx-lg-5 mx-md-3', :style => "text-align: left; font-size: 16px; font-family: 'Noto Sans JP', sans-serif; text-decoration: none" }) do |f| %>
    <div class="row">
      <div class="col-lg-12">
        <%# <%= f.input :account_number, placeholder: 'XXXXXX' %> 
        <%# <%= f.association :user %>
      </div>
    </div>
    <%= f.button :submit, "Criar Conta", class: 'button-7 mb-2'%>
  <%= link_to "Cancel", bank_accounts_path, class: "button-danger"%>
  <% end %>
</div>