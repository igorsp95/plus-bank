<div class="container">
  <div class="row d-flex justify-content-center">
    <div class="my-card col-xs-12 col-sm-6 col-sm-offset-4">
      <%= simple_form_for(@bank_account, :html => { :class => 'form-horizontal items-form', :style => "text-align: left; font-size: 16px; font-family: 'Noto Sans JP', sans-serif; text-decoration: none" }) do |f| %>
        <div class="row">
          <div class="col-lg-12">
            <%# <%= f.input :account_number, placeholder: 'XXXXXX' %> 
            <%# <%= f.association :user %>
          </div>
        </div>
        <%= f.button :submit, "Create New Account", class: 'button-7 mb-2'%>
      <%= link_to "Cancel", bank_accounts_path, class: "button-danger"%>
      <% end %>
    </div>
  </div>
</div>